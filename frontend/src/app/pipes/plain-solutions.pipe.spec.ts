import { PlainSolutionsPipe } from './plain-solutions.pipe';

describe('PlainSolutionsPipe', () => {
  it('create an instance', () => {
    const pipe = new PlainSolutionsPipe();
    expect(pipe).toBeTruthy();
  });
});
