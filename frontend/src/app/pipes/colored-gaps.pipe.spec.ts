import { ColoredGapsPipe } from './colored-gaps.pipe';

describe('ColoredGapsPipe', () => {
  it('create an instance', () => {
    const pipe = new ColoredGapsPipe();
    expect(pipe).toBeTruthy();
  });
});
